<%= form_for(@betraege) do |f| %>
  <% if @betraege.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@betraege.errors.count, "error") %> prohibited this betraege from being saved:</h2>

      <ul>
      <% @betraege.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :betrag %><br />
    <%= f.text_field :betrag %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
